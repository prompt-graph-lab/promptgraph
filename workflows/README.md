# Common Workflow Presets

Place shared ComfyUI workflow JSON presets in this directory.

PromptGraph Pro discovers `*.json` files here automatically and can use a selected preset when the project-specific workflow path is missing.
